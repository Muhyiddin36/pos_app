# Blokir akses langsung ke seluruh isi folder app/ (hanya index.php di root yang boleh memanggilnya)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
